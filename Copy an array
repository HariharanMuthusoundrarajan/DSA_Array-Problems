//4.Copy an array
// Input : arr = {5, 10, 15}   Output: Copied array: 5 10 15
public static int[] copyArray(int[] arr) {
    int[] copy = new int[arr.length];
    for (int i = 0; i < arr.length; i++) {
        copy[i] = arr[i];
    }
    return copy;
}
