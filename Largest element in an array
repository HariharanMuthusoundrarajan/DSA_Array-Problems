// 9.Find the largest Number
// Input: arr = {1, 4, 8, 6}  Output:Largest: 8


public static int findLargest(int[] arr) {
    int max = arr[0];
    for (int i = 1; i < arr.length; i++) {
        if (arr[i] > max) max = arr[i];
    }
    return max;
}
