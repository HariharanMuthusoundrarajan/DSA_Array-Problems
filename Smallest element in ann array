//8. Find the smallest number
// Input:arr = {9, 3, 7, 1} Output: Smallest: 1
public static int findSmallest(int[] arr) {
    int min = arr[0];
    for (int i = 1; i < arr.length; i++) {
        if (arr[i] < min) min = arr[i];
    }
    return min;
}
