// 6.Search an element in an array
// Input:arr = {2, 4, 6, 8}, key = 6  Output: Element found at index: 2


public static int searchElement(int[] arr, int key) {
    for (int i = 0; i < arr.length; i++) {
        if (arr[i] == key) return i;
    }
    return -1; // Not found
}
